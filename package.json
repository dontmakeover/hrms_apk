{
  "name": "talent-factor-hrms",
  "version": "1.0.0",
  "description": "Talent Factor HRMS Mobile App",
  "main": "index.js",
  "scripts": {
    "build": "echo Build complete",
    "cap:init": "npx cap init \"Talent Factor HRMS\" com.talentfactor.hrms --web-dir=.",
    "cap:add:android": "npx cap add android",
    "cap:sync": "npx cap sync",
    "cap:open:android": "npx cap open android",
    "android:build": "cd android && gradlew.bat assembleDebug"
  },
  "keywords": [
    "hrms",
    "mobile",
    "android"
  ],
  "author": "Talent Factor",
  "license": "MIT",
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}